# 已知限制与后续计划

## 已知限制

1. 当前有效真实内容只有重新采集的小红书三文鱼波奇饭 3 篇、抖音奶茶 2 篇，只覆盖 2 个品类。
2. 小红书 `xsec_token` 属于可失效的页面定位参数，已按用户要求保留用于本地演示；不应把含该参数的数据包直接发布到公共仓库。
3. 真实候选池已通过独立低频命令完成 27 条“先存后排”验证，但 food 页面实时按钮尚未自动串联该命令；当前详情采集仍使用原通用采集排序路径。
4. 真实内容的活人感表达仍待人工审核；推荐理由不能因此宣称已完成真实人工闭环。
5. 金标准库存为 5 篇待标内容、0 篇已确认，未达到 24 篇/60 句/第二标注 20%。
6. 没有调用外部模型，Prompt 对比仅为规则契约回归。
7. ASR 会误识别菜名、品牌和方言；本轮已有明显“ 三文鱼/波奇饭 ”误转写，需要人工校正但不能覆盖原始转写。
8. 评论前 10 条不等于高赞、最新和描述价值三类完整采样。

## 后续优先级

P0：继续补齐抖音样本，并增强详情打开/重试校验；当前重新采集的 2 条均已通过页面噪声检查。

P0：补齐小红书 12、抖音 12，覆盖奶茶、咖啡、轻食、甜品、小吃；由第一标注人完成 60 句，再由第二标注人独立覆盖至少 20%。

P1：把已验证的真实候选池命令自动接入 food 页面实时流程，并补真实 Top 5 人工重合；当前随机基线已运行，但缺少人工 Top 5。

P1：接入现有模型调用层时只读环境变量，不读取或复制 `.env`；记录模型、温度、截断、耗时和失败原因，同一金标准比较 v0/v1。

P2：评论采样增加高赞、最新、食品描述三队列；高相似内容只标记，不自动删除。

达到数据库、队列或大型采集框架触发条件前，继续保持单机 JSONL/Markdown 架构。
