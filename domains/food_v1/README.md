# food_v1

这是 SocialReader 通用采集底座上的可插拔美食领域方案。原始 Markdown 仍由现有采集流程保存；本模块只生成带证据回链的衍生 JSONL 和报告。

“热门”仅指同平台、同关键词、同一次可见候选池中的相对热门；跨平台不相加原始互动量。模型或规则只抽取候选，人工审核结果单独保存。

默认输出：`D:\ObsidianVault\采集\美食情报\YYYY-MM-DD\<run_id>`。
